<h1><%= @task.title%> </h1>
<h3><%= @task.details%></h3>

  <p>task completed ? <%= @task.completed ? "yes" : "to do"%></p>


<%= link_to "back to list", tasks_path %>
<br>
<%= link_to "edit", edit_task_path(@task) %>
<br>
<%= link_to "Delete #{@task.title}",
            task_path(@task),
            method: :delete,
            data: { confirm: "Are you sure?" } %>
